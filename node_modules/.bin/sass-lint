#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sass-lint/bin/sass-lint.js" "$@"
  ret=$?
else 
  node  "$basedir/../sass-lint/bin/sass-lint.js" "$@"
  ret=$?
fi
exit $ret
