#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gonzales-pe-sl/bin/gonzales.js" "$@"
  ret=$?
else 
  node  "$basedir/../gonzales-pe-sl/bin/gonzales.js" "$@"
  ret=$?
fi
exit $ret
